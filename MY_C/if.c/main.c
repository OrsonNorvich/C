#include <stdio.h>
#include <stdlib.h>

int main()
{
    int ten, dig;
    scanf("%d%d", &ten, &dig);
    if (ten==1&&dig==1)
        {printf("You enter number is eleven");}
    if (ten==1&&dig==2)
        printf("You enter number is twelve");
    return 0;
}
